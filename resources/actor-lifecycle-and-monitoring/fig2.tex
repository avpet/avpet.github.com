%&latex
\documentclass[10pt,landscape,ansibpaper]{article}
\usepackage[margin=2em]{geometry}
\usepackage{tikz}
\usetikzlibrary{trees,arrows}
\tikzstyle{every node}=[draw=black, thin, minimum height=3em]

\begin{document}
\footnotesize
\begin{tikzpicture}[
    supervisor/.style={%
        text centered, text width=6em,
        text=black
    },
    teammate/.style={%
        text centered, text width=6em,
        text=black
    },
    subordinate/.style={%
        grow=down,
        xshift=-3.2em, % Horizontal position of the child node
        text centered, text width=6em,
        edge from parent path={(\tikzparentnode.205) |- (\tikzchildnode.west)}
    },
    level1/.style ={level distance=4em,anchor=west},
    level2/.style ={level distance=8em,anchor=west},
    level3/.style ={level distance=12em,anchor=west},
    level4/.style ={level distance=16em,anchor=west},
    level 1/.style={edge from parent fork down,sibling distance=18em,level distance=5em},
    level 2/.style={edge from parent fork down,sibling distance=18em}
]
    \node[anchor=south,supervisor](super){/}[]
    child{node [teammate] {user} edge from parent[<-]
        child{node [teammate] {top-1} edge from parent[<-]
            child[teammate] {node {child-1} edge from parent[<-]}
            child[teammate] {node {child-2} edge from parent[<-]}}
        child{node [teammate] {top-2} edge from parent[<-]}
    }
    child{node [teammate] {system} edge from parent[<-]};
\end{tikzpicture}
\end{document}

